{
  "name": "jaipur-gadget",
  "version": "1.0.0",
  "description": "Jaipur Gadget E-commerce Platform",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install"
  },
  "private": true
}

